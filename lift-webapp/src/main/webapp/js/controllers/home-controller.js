'use strict';

Skeleton.Controllers.controller('HomeController', function ($scope) {
    $scope.welcomeMessage = "Hello and welcome to the home controller.  " +
        "Put dynamic data onto the scope of this controller to be displayed on the page."
});